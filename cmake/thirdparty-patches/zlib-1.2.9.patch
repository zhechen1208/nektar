--- zlib-1.2.9/zutil.h
+++ zlib-1.2.9/zutil.h
@@ -137,7 +137,9 @@
 #      include <unix.h> /* for fdopen */
 #    else
 #      ifndef fdopen
-#        define fdopen(fd,mode) NULL /* No fdopen() */
+#        ifndef __APPLE__
+#          define fdopen(fd,mode) NULL /* No fdopen() */
+#        endif
 #      endif
 #    endif
 #  endif
